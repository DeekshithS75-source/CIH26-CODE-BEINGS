class AppConstants {
  static const appName = 'FarmEdge Monitor';
  static const defaultBaseUrl = 'http://localhost:3001';
  static const minPollingSeconds = 1;
  static const maxPollingSeconds = 60;
  static const chartWindow = 50;
}
